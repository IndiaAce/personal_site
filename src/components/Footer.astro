<footer class="ftr">
  <div class="ftr__col">
    <span class="ftr__brand">LUKE WESCOTT</span>
    <span>MSc in AI/ML</span>
  </div>
  <div class="ftr__col">
    <span>THREAT DETECTION ENGINEER</span>
    <a href="https://github.com/IndiaAce" target="_blank" rel="noopener noreferrer"
      >github.com/IndiaAce</a
    >
  </div>
  <div class="ftr__col">
    <span>EST. SUBLIME SECURITY</span>
    <span>EN · FR (B1)</span>
  </div>
</footer>

<style>
  .ftr {
    position: relative;
    z-index: 1;
    border-top: var(--border);
    background: var(--bg);
    padding: 22px 6vw 30px;
    font-family: var(--font-mono);
    font-size: 12px;
    letter-spacing: .14em;
    text-transform: uppercase;
    color: var(--ink-soft);
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 24px;
  }
  /* each section keeps its left / middle / right anchor, but its two lines
     are centered relative to each other (shorter line under the longer one) */
  .ftr__col {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 8px;
  }
  .ftr__brand { color: var(--ink); font-weight: 700; }
  .ftr__col a { color: var(--accent); text-decoration: none; }
  .ftr__col a:hover { text-decoration: underline; }

  /* mobile (≤720px): centered stacked lines, sits above the bottom tab bar */
  @media (max-width: 720px) {
    .ftr {
      flex-direction: column;
      align-items: center;
      gap: 14px;
      padding: 26px 22px 24px;
      font-size: 10.5px;
      letter-spacing: .16em;
    }
  }
</style>
